import { styled } from 'styled-components';

export const StyledPageTitle = styled.h1`
  margin: 0;
  padding: 1rem;
  text-align: center;
`;

export const StyledViewportContainer = styled.div`
  display: flex;
  flex-direction: column;
  height: 100%;
  width: 100%;
  overflow: hidden;
`;
